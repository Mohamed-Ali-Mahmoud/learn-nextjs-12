// check out the globals.css file in the styles folder to see comments for how to import this

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
